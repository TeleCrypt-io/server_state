# CI-only secret-mount overlay. The rendered-Compose validator separately proves the canonical
# production mounts; this runtime proof exercises only Synapse's canonical secret grants.
services:
  synapse:
    volumes: !reset []
    tmpfs: !reset []
