name: validate

on:
  push:
    branches: [main]
    tags: ["server-state-*"]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Verify OAuth-only Matrix login ingress
        run: |
          python3 - <<'PY'
          from pathlib import Path

          caddy = Path("Caddyfile").read_text(encoding="utf-8")
          login_deny = "@matrix_compat_login path_regexp ^/_matrix/client/(.*)/login/?$"
          compat_allow = "@mas_compat path_regexp ^/_matrix/client/(.*)/(logout|refresh)/?$"
          assert login_deny in caddy
          assert compat_allow in caddy
          assert caddy.index(login_deny) < caddy.index(compat_allow)
          assert "(login|logout|refresh)" not in caddy
          assert "@mas path /auth /auth/*" in caddy
          PY

      - name: Verify canonical and legacy Plan action ingress
        run: |
          python3 - <<'PY'
          from pathlib import Path

          caddy = Path("Caddyfile").read_text(encoding="utf-8")
          plan_routes = "@plan path /plan /plan/* /api/plan /api/plan/* /api/team /api/team/*"
          assert plan_routes in caddy
          start = caddy.index(plan_routes)
          route = caddy[start:caddy.index("\n\t}", start) + 3]
          assert "reverse_proxy steward:9012" in route
          assert "reverse_proxy cashier:9011" not in route
          PY

      - name: Verify exact Controlplane release selection
        run: |
          python3 - <<'PY'
          import re
          from pathlib import Path

          compose = Path("compose.yml").read_text(encoding="utf-8")
          expected = "ghcr.io/telecrypt-io/controlplane:0.3.19"
          for service in ("controlplane", "janitor", "steward"):
              block = re.search(
                  rf"^  {service}:\n(?P<body>.*?)(?=^  [a-z][a-z0-9_-]*:|\Z)",
                  compose,
                  flags=re.MULTILINE | re.DOTALL,
              )
              assert block, f"missing {service} service"
              assert f"image: {expected}" in block.group("body")
          images = re.findall(r"image: (ghcr.io/telecrypt-io/controlplane:[^\s]+)", compose)
          assert images == [expected] * 3, images
          PY

      - name: Validate Compose
        env:
          TELECRYPT_SECRETS_DIR: /tmp/telecrypt-secrets
          TELECRYPT_DATA_DIR: /tmp/telecrypt-data
        run: |
          mkdir -p "$TELECRYPT_SECRETS_DIR" "$TELECRYPT_DATA_DIR"
          touch "$TELECRYPT_SECRETS_DIR/locker.secrets.env"
          touch "$TELECRYPT_SECRETS_DIR/steward.secrets.env"
          touch "$TELECRYPT_SECRETS_DIR/cashier.secrets.env"
          printf '%s\n' 'DODO_WEBHOOK_PATH=/webhook-dodo-ci-only' > "$TELECRYPT_SECRETS_DIR/ingress.secrets.env"
          docker compose --env-file .env.example config --quiet

      - name: Validate Caddy
        run: |
          docker run --rm --env DODO_WEBHOOK_PATH=/webhook-dodo-ci-only \
            --volume "$PWD/Caddyfile:/etc/caddy/Caddyfile:ro" \
            docker.io/caddy:2.11.4-alpine caddy validate --config /etc/caddy/Caddyfile

      - name: Verify pinned standalone Synapse image
        # A server PR is opened before the coordinated component releases exist. The image-builder
        # workflow tests this target; server verifies the published exact tag on main/tag runs.
        if: github.event_name != 'pull_request'
        run: >-
          docker run --rm
          --entrypoint python
          ghcr.io/telecrypt-io/telecrypt-synapse:1.155-tc1
          -c 'from tier_controller import TierController; print(TierController.__name__)'
